import Dashboard from "../page";

/**
 * /dashboard route - simply exports the same dashboard content from root page.
 * This allows OAuth callbacks to redirect to /dashboard which is clearer than /.
 */
export default Dashboard;
