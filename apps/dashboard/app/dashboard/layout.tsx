/**
 * Layout for dashboard sub-routes (profile, connections).
 * The DashboardShell is provided by the root layout.
 */
export default function DashboardLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
